---
const acceptLanguage = Astro.request.headers.get("accept-language") || "";
console.log(acceptLanguage);
const preferredLang = acceptLanguage.includes("es") ? "es" : "en";

return Astro.redirect(`/${preferredLang}/`);
---
